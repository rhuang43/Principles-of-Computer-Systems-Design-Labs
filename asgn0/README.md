#Assignment 0 directory
```
./                        
|- ...
|- Author: Raymond Huang
|- Student Id: W1689275
|- Files:hello.c, Makefile, attestation.txt, README.md
    |-hello.c is the source file that contains all the code to implements stdout "Hello World!"
    |-Makefile is the file that where it creates multiple targets to compile and remove the objects and binary files.
    |-attestation.txdt is the text file that contains the text "I successfully created and am using an Ubuntu 22.04 virtual machine."
    |-README.md contains the documentation for each file in the folder.
    |- ...
```